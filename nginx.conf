# ====================================================================
# 🌐 NGINX - CONFIGURAÇÃO PRINCIPAL DIGIURBAN
# ====================================================================
# Configuração principal para usuário não-privilegiado
# ====================================================================

# Configuração para usuário não-root  
worker_processes auto;
pid /tmp/nginx.pid;
error_log /tmp/error.log;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    # Configurações temporárias para usuário não-root
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path       /tmp/proxy_temp_path;
    fastcgi_temp_path     /tmp/fastcgi_temp;
    uwsgi_temp_path       /tmp/uwsgi_temp;
    scgi_temp_path        /tmp/scgi_temp;
    
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # Configurações de segurança e performance
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off;
    
    # Logs seguros
    access_log /tmp/access.log;
    
    # Incluir configuração do server
    include /etc/nginx/conf.d/*.conf;
}